Usamos: abstracción,array list, try catch, herencia , ficheros , interfaces

El proyecto es de una Tienda Online y esta compuesto por:


Main-
    producto-
        (Atributos)
            -String nombre;
            -int precio;
            -int stock;
            -String marca;
        productoAlimenticio-(extiende de producto)
           (Atributos)
                boolean comida;
                boolean bebida;
                Srting caducidad;


        productoElectronico-(extiende de producto)
           (Atributos)
                int numeroDeSerie;
                boolean camara;
                string sistemaOperativo;


        productoTextil-(extiende de producto)
           (Atributos)
                char talla;
                String tipo;
                boolean segundaMano;

    usuario-(clase abstracta)
            (Atributos)
                String nombre;
                String apellido;
                String direccion;
                char sexo;

        cliente-(extiende de usuario)
              (atributos)
                   int id;
                   int saldo;


    Cesta-

    decuentos(intefaz)

        descuentosCantidad(extiende de descueto)

        descuentoPrimeraCompra(extiende de descuento)



        ***CAMBIOS***
        writer.write("Ha realizado un compra el cliente: "+cliente1.nombre+" con el ID "+cliente1.id);---si no puede comprarlo tiene que salir otra cosa
        poder usar el total de precio mas impuestos en otras clases
        Mirar de añadir el gasto total en el fichero